'use strict';

angular.module('shikenApp')
    .factory('Platform', function ($resource, DateUtils) {
        return $resource('api/platforms/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
